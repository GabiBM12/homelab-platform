---
all:
  children:
    management:
      hosts:
        management01:
          # Replace these values before the first run.
          ansible_host: REPLACE_WITH_MANAGEMENT_LAPTOP_IP
          ansible_user: vm_user
